apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: hello 
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: hello
  minReplicas: 1
  maxReplicas: 3 
  targetCPUUtilizationPercentage: 50
